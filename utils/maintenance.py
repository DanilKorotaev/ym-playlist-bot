"""
Утилиты для работы с режимом технических работ.
"""
import os
import logging
from typing import Optional

logger = logging.getLogger(__name__)

# Сообщение о технических работах
MAINTENANCE_MESSAGE = (
    "🔧 Ведутся технические работы. Бот временно недоступен.\n\n"
    "Приносим извинения за неудобства."
)


def is_maintenance_mode() -> bool:
    """
    Проверяет, включен ли режим технических работ.
    
    Режим включается через переменную окружения MAINTENANCE_MODE.
    Поддерживаются значения: true, True, TRUE, 1, yes, Yes, YES
    
    Returns:
        True, если режим техработ включен, иначе False
    """
    maintenance_mode = os.getenv("MAINTENANCE_MODE", "false").lower()
    
    # Поддерживаем различные варианты значения true
    return maintenance_mode in ("true", "1", "yes", "on")


def get_maintenance_message() -> str:
    """
    Возвращает сообщение о технических работах.
    
    Returns:
        Текст сообщения о техработах
    """
    return MAINTENANCE_MESSAGE

