# Команды бота

## Основные команды

### `/start`
**Описание**: Приветствие и помощь. Показывает список доступных команд и информацию об активном плейлисте.

**Использование**: 
- `/start` — показать помощь
- `/start <токен>` — получить доступ к плейлисту по токену шаринга

**Пример**: `/start` или `/start abc123xyz`

---

### `/create_playlist`
**Описание**: Создать новый плейлист в Яндекс.Музыке.

**Режим работы**: Интерактивный диалог (FSM). Бот попросит ввести название плейлиста.

**Ограничения**: 
- Лимит плейлистов на пользователя: по умолчанию 2 (настраивается через переменную окружения `PLAYLIST_LIMIT`)
- При достижении лимита бот покажет сообщение с информацией о текущем использовании и предложит удалить один из существующих плейлистов

**Возвращает**: 
- Ссылку для шаринга плейлиста
- Устанавливает созданный плейлист как активный

**Пример**: `/create_playlist` (запустит интерактивный диалог)

---

### `/my_playlists`
**Описание**: Показать список плейлистов, созданных пользователем.

**Возвращает**: 
- Список плейлистов с inline-кнопками для выбора
- Активный плейлист отмечен значком 🎵
- Информация о текущем использовании лимита (например, "📊 1/2 плейлистов")

**Пример**: `/my_playlists`

---

### `/shared_playlists`
**Описание**: Показать список плейлистов, куда пользователь добавляет треки (но не является создателем).

**Возвращает**: Список плейлистов с inline-кнопками для выбора. Активный плейлист отмечен значком 🎵.

**Пример**: `/shared_playlists`

---

### `/list`
**Описание**: Показать список треков в активном плейлисте.

**Возвращает**: Пронумерованный список треков с названиями и исполнителями.

**Пример**: `/list`

---

### `/playlist_info`
**Описание**: Показать информацию об активном плейлисте.

**Возвращает**: 
- Название плейлиста
- Статус пользователя (создатель/участник)
- Количество треков
- Информацию о том, куда добавляются треки (в начало или в конец)
- Ссылку на плейлист в Яндекс.Музыке
- Ссылку для шаринга (если пользователь — создатель)
- Inline-кнопки для действий (изменить название, удалить плейлист, удалить трек)

**Пример**: `/playlist_info`

---

### `/set_token`
**Описание**: Установить свой токен Яндекс.Музыки для создания плейлистов в своем аккаунте.

**Режим работы**: Интерактивный диалог (FSM). Бот попросит ввести токен.

**⚠️ ВНИМАНИЕ**: Передача токена боту происходит на ваш страх и риск!

**Как получить токен**: https://yandex-music.readthedocs.io/en/main/token.html

**Пример**: `/set_token` (запустит интерактивный диалог)

---

### `/edit_name`
**Описание**: Изменить название плейлиста в Яндекс.Музыке (только для создателя).

**Режим работы**: Интерактивный диалог (FSM). Бот попросит ввести новое название плейлиста (максимум 100 символов).

**Требования**: Пользователь должен быть создателем активного плейлиста.

**Что происходит**: Название изменяется в Яндекс.Музыке через API и автоматически синхронизируется с базой данных бота.

**Альтернативный способ**: Использовать кнопку "✏️ Изменить название" в `/playlist_info`

**Пример**: `/edit_name` (запустит интерактивный диалог)

---

### `/delete_playlist`
**Описание**: Удалить плейлист из базы данных бота (только для создателя).

**⚠️ ВНИМАНИЕ**: Плейлист удаляется только из базы данных бота. В Яндекс.Музыке плейлист остается, но бот теряет связь с ним.

**Требования**: Пользователь должен быть создателем активного плейлиста.

**Альтернативный способ**: Использовать кнопку "🗑️ Удалить плейлист" в `/playlist_info`

**Пример**: `/delete_playlist`

---

### `/delete_track`
**Описание**: Удалить трек из активного плейлиста по номеру.

**Режим работы**: Интерактивный диалог (FSM). Бот покажет количество треков и попросит ввести номер трека для удаления (начиная с 1).

**Требования**: Пользователь должен иметь права на редактирование плейлиста.

**Альтернативный способ**: Использовать кнопку "🗑️ Удалить трек" в `/playlist_info` (доступна только если есть треки и есть права на редактирование)

**Пример**: `/delete_track` (запустит интерактивный диалог)

---

## Главное меню

Бот предоставляет удобную клавиатуру с основными действиями:

- **📁 Мои плейлисты** — показать плейлисты, которые вы создали
- **📂 Общие плейлисты** — показать плейлисты, куда вы добавляете
- **➕ Создать плейлист** — создать новый плейлист (интерактивный режим)
- **📋 Список треков** — показать треки в активном плейлисте
- **ℹ️ Информация** — информация об активном плейлисте
- **🏠 Главное меню** — вернуться в главное меню

---

## Интерактивные диалоги (FSM)

Бот поддерживает интерактивные диалоги для следующих операций:

### Создание плейлиста
1. Нажмите кнопку "➕ Создать плейлист" или отправьте `/create_playlist`
2. Бот попросит ввести название плейлиста
3. Введите название (максимум 100 символов)
4. Бот проверит лимит плейлистов (по умолчанию: 2)
5. Если лимит не достигнут, бот создаст плейлист и покажет ссылку для шаринга
6. Если лимит достигнут, бот покажет сообщение с информацией о текущем использовании и предложит удалить один из существующих плейлистов

**Отмена**: Нажмите кнопку "❌ Отмена" или отправьте `/cancel` или `/start`

### Установка токена
1. Отправьте `/set_token`
2. Бот попросит ввести токен
3. Введите ваш токен Яндекс.Музыки
4. Бот проверит и сохранит токен

**Отмена**: Нажмите кнопку "❌ Отмена" или отправьте `/cancel` или `/start`

### Изменение названия плейлиста
1. Откройте `/playlist_info` и нажмите кнопку "✏️ Изменить название" или отправьте `/edit_name`
2. Бот попросит ввести новое название
3. Введите новое название (максимум 100 символов)
4. Бот обновит название плейлиста в Яндекс.Музыке и синхронизирует с базой данных

**Отмена**: Нажмите кнопку "❌ Отмена" или отправьте `/cancel` или `/start`

### Удаление трека
1. Откройте `/playlist_info` и нажмите кнопку "🗑️ Удалить трек" или отправьте `/delete_track`
2. Бот покажет количество треков и попросит ввести номер трека
3. Введите номер трека (от 1 до количества треков)
4. Бот удалит трек из плейлиста

**Отмена**: Нажмите кнопку "❌ Отмена" или отправьте `/cancel` или `/start`

---

## Работа со ссылками

Бот автоматически распознает ссылки на треки, альбомы и плейлисты Яндекс.Музыки и добавляет их в активный плейлист. Место добавления (в начало или в конец) определяется настройкой плейлиста, которую может изменить только создатель плейлиста.

### Поддерживаемые форматы

**Треки**:
- `https://music.yandex.ru/track/123456`
- `https://music.yandex.ru/track/abc123-def456`
- Просто номер трека: `123456`

**Альбомы**:
- `https://music.yandex.ru/album/123456`
- `https://music.yandex.ru/album/abc123-def456`

**Плейлисты**:
- `https://music.yandex.ru/users/username/playlists/123`
- `https://music.yandex.ru/playlists/123`

**Токены шаринга**:
- `https://t.me/botname?start=TOKEN`
- Просто токен: `TOKEN`

---

## Inline-кнопки

### Выбор плейлиста
При просмотре списка плейлистов (`/my_playlists`, `/shared_playlists`) бот показывает inline-кнопки для быстрого выбора плейлиста. Нажатие на кнопку устанавливает выбранный плейлист как активный.

### Действия с плейлистом
В `/playlist_info` доступны следующие inline-кнопки (в зависимости от прав доступа):

- **✏️ Изменить название** — только для создателя плейлиста
- **🗑️ Удалить плейлист** — только для создателя плейлиста
- **🗑️ Удалить трек** — для пользователей с правами редактирования, только если в плейлисте есть треки

### Редактирование плейлиста
При нажатии на кнопку "✏️ Редактировать" в `/playlist_info` (доступна только для создателя) открывается меню редактирования с дополнительными опциями:

- **✏️ Изменить имя** — изменение названия плейлиста
- **🖼️ Изменить/установить картинку** — установка обложки плейлиста
- **📍 Добавление треков: в начало/в конец** — переключение места добавления треков
  - По умолчанию треки добавляются в конец плейлиста
  - Можно переключить на добавление в начало
  - Настройка сохраняется для каждого плейлиста отдельно
- **🗑️ Удалить плейлист** — удаление плейлиста из базы данных бота
- **🗑️ Удалить трек** — удаление трека из плейлиста

---

## Активный плейлист

Бот автоматически определяет активный плейлист для каждого пользователя:
1. Если пользователь выбрал плейлист через inline-кнопку — используется он
2. Если пользователь создал плейлист — он становится активным
3. Если пользователь получил доступ к плейлисту через ссылку — он становится активным
4. Иначе используется первый доступный плейлист пользователя

Активный плейлист отображается в главном меню и используется для:
- Добавления треков (при отправке ссылок)
- Команды `/list`
- Команды `/playlist_info`
- Команды `/delete_track`

---

## Права доступа

### Создатель плейлиста
- ✅ Полный доступ (добавление, редактирование, удаление треков)
- ✅ Изменение названия плейлиста в Яндекс.Музыке
- ✅ Удаление плейлиста из базы данных
- ✅ Просмотр ссылки для шаринга
- ✅ Просмотр ссылки на плейлист в Яндекс.Музыке

### Участник плейлиста
- ✅ Добавление треков
- ✅ Просмотр списка треков
- ✅ Просмотр информации о плейлисте
- ✅ Просмотр ссылки на плейлист в Яндекс.Музыке
- ❌ Редактирование названия
- ❌ Удаление плейлиста
- ❌ Удаление треков (требуются права на редактирование)

---

## Примеры использования

### Создание и шаринг плейлиста
```
1. Нажмите "➕ Создать плейлист" или отправьте /create_playlist
   → Бот попросит ввести название
2. Введите название: "Вечеринка"
   → Бот создает плейлист и отправляет ссылку для шаринга
3. Отправка ссылки друзьям
   → Друзья переходят по ссылке и получают доступ
4. Друзья отправляют ссылки на треки
   → Треки автоматически добавляются в плейлист
```

### Использование своего токена
```
1. Получить токен на https://yandex-music.readthedocs.io/en/main/token.html
2. Отправить /set_token
   → Бот попросит ввести токен
3. Ввести токен
   → Бот сохраняет токен
4. Создать плейлист
   → Плейлист создается в вашем аккаунте Яндекс.Музыки
```

### Управление несколькими плейлистами
```
1. Нажать "📁 Мои плейлисты"
   → Выбрать плейлист через inline-кнопку
2. Отправить ссылку на трек
   → Трек добавляется в выбранный плейлист
3. Нажать "📋 Список треков"
   → Просмотр треков в активном плейлисте
4. Нажать "ℹ️ Информация"
   → Просмотр информации и действий с плейлистом
```

### Удаление трека
```
1. Нажать "ℹ️ Информация" или отправить /playlist_info
2. Нажать кнопку "🗑️ Удалить трек"
   → Бот покажет количество треков и попросит ввести номер
3. Ввести номер трека (например, 5)
   → Бот удалит трек из плейлиста
```
