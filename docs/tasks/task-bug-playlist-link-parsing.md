# Исправление парсинга ссылок на плейлисты и получения плейлистов по ссылке

**Статус**: 🔍 Исследование  
**Приоритет**: 🔴 КРИТИЧНО  
**Категория**: Исправление багов

## Проблема

При добавлении треков из плейлиста по ссылке возникает ошибка `not-found` при вызове `users_playlists(pid)` без owner.

### Симптомы

В логах:
```
2025-12-04 19:24:42,275 - yandex_music.client - DEBUG - Entering: users_playlists
2025-12-04 19:24:42,405 - services.yandex_service - DEBUG - users_playlists(pid) failed: {'name': 'not-found', 'message': 'Requested action for /users/1191376640/playlists/5cf4b069-b6ea-4558-a94e-6a44521b495d was not found'}
```

Бот отвечает:
```
❌ Не удалось получить плейлист: Не удалось получить плейлист 5cf4b069-b6ea-4558-a94e-6a44521b495d
💡 Проверьте правильность ссылки.
```

## Описание

При добавлении треков из плейлиста по ссылке:
1. Ссылка парсится через `parse_playlist_link()`
2. Если ссылка в формате `https://music.yandex.ru/playlists/{uuid}`, owner не извлекается (возвращается `None`)
3. В `YandexService.get_playlist()` сначала пытается вызвать `users_playlists(pid, owner)`, но owner = None, поэтому пропускается
4. Затем вызывается `users_playlists(pid)` без owner, что приводит к ошибке `not-found`

### Возможные причины

1. **API требует owner для некоторых плейлистов**: Для плейлистов с UUID может требоваться указание owner при вызове `users_playlists`
2. **Неправильный парсинг ссылок**: Ссылки формата `https://music.yandex.ru/playlists/{uuid}` не содержат owner в URL, но API может требовать его
3. **Формат ID плейлиста**: UUID плейлиста может быть не тем идентификатором, который принимает API `users_playlists` (может требоваться `kind` вместо UUID)
4. **Отсутствие обработки разных форматов ссылок**: API может поддерживать разные форматы идентификаторов плейлистов

## Задачи

- [ ] Исследовать работу API `users_playlists` с разными форматами ID плейлистов
- [ ] Проверить, требуется ли owner для получения плейлиста по UUID
- [ ] Изучить, как получить owner из UUID плейлиста (если возможно)
- [ ] Проверить, какие форматы ссылок поддерживает Яндекс.Музыка
- [ ] Исследовать альтернативные методы получения плейлиста по UUID
- [ ] Улучшить парсинг ссылок для извлечения owner, если он доступен
- [ ] Улучшить метод `YandexService.get_playlist()` для обработки разных форматов ID
- [ ] Добавить обработку ошибок с более понятными сообщениями
- [ ] Протестировать исправления с разными форматами ссылок

## Исследование

### Вопросы для исследования

1. **Формат ID плейлиста в API**:
   - Какой формат ID использует `users_playlists`? (kind, UUID, или оба?)
   - Требуется ли owner для получения плейлиста по UUID?
   - Можно ли получить плейлист по UUID без owner?

2. **Форматы ссылок Яндекс.Музыки**:
   - Какие форматы ссылок поддерживаются?
   - `https://music.yandex.ru/users/{owner}/playlists/{kind}` - старый формат
   - `https://music.yandex.ru/playlists/{uuid}` - новый формат
   - Есть ли другие форматы?

3. **Получение owner из UUID**:
   - Можно ли получить owner плейлиста по UUID через API?
   - Есть ли метод для получения информации о плейлисте по UUID?
   - Можно ли использовать другие методы API для получения плейлиста?

4. **Обработка ошибок**:
   - Какие ошибки может возвращать `users_playlists`?
   - Как различать "плейлист не найден" и "недостаточно прав"?
   - Нужно ли пробовать разные варианты вызова API?

### План исследования

1. Изучить документацию библиотеки `yandex-music` и API Яндекс.Музыки
2. Проверить, какие методы доступны для получения плейлиста по UUID
3. Протестировать разные варианты вызова `users_playlists`:
   - `users_playlists(uuid)` без owner
   - `users_playlists(uuid, owner)` с owner
   - `users_playlists(kind, owner)` с kind и owner
4. Проверить, можно ли получить owner из UUID через другие методы API
5. Изучить примеры использования API в коде проекта

## Реализация

### Варианты решения

#### Вариант 1: Улучшение парсинга ссылок
- Если ссылка в формате `https://music.yandex.ru/playlists/{uuid}`, попытаться получить owner через API
- Сохранить mapping UUID -> owner для кэширования

#### Вариант 2: Улучшение метода `get_playlist`
- Если owner не указан, попытаться получить плейлист разными способами:
  - Сначала попробовать `users_playlists(uuid)` без owner
  - Если не получилось, попробовать найти owner через другие методы API
  - Если owner найден, попробовать `users_playlists(uuid, owner)`

#### Вариант 3: Использование альтернативных методов API
- Проверить, есть ли другие методы для получения плейлиста по UUID
- Использовать методы, которые не требуют owner

#### Вариант 4: Комбинированный подход
- Улучшить парсинг ссылок для извлечения максимальной информации
- Улучшить метод `get_playlist` для обработки разных сценариев
- Добавить fallback-механизмы для получения плейлиста

### Детали реализации

1. **Улучшение `parse_playlist_link`**:
   - Проверить, можно ли извлечь owner из других частей ссылки
   - Добавить поддержку дополнительных форматов ссылок

2. **Улучшение `YandexService.get_playlist`**:
   - Добавить логику для определения формата ID (UUID vs kind)
   - Попытаться получить owner, если он не указан
   - Добавить несколько попыток с разными вариантами вызова API
   - Улучшить обработку ошибок с более понятными сообщениями

3. **Обработка ошибок**:
   - Различать разные типы ошибок (not-found, forbidden, etc.)
   - Предоставлять более понятные сообщения пользователю
   - Логировать детальную информацию для отладки

## Связанные файлы

- `services/link_parser.py` - функция `parse_playlist_link`
- `services/yandex_service.py` - метод `get_playlist`
- `handlers/messages.py` - обработчик `add_command` (строки 186-218)

## Примечания

- UUID плейлиста может отличаться от `kind` плейлиста
- API может требовать owner для приватных плейлистов
- Некоторые плейлисты могут быть недоступны без авторизации владельца

