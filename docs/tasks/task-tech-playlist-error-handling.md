# Улучшение обработки ошибок при создании и редактировании плейлистов

**Статус**: 📋 Запланировано  
**Приоритет**: 🔴 Высокий (критично для безопасности аккаунта)  
**Категория**: Технические улучшения  
**Связанное исследование**: [playlist_moderation_research.md](../research/playlist_moderation_research.md)

## Проблема

Текущая обработка ошибок при создании и редактировании плейлистов недостаточно детализирована:
- Все ошибки обрабатываются общим образом
- Нет различения ошибок модерации от других типов ошибок
- Пользователи не получают понятных сообщений
- Нет возможности повторить попытку с другим названием при ошибке модерации
- Риск блокировки аккаунта при множественных попытках создать плейлист с некорректным названием

## Цель

Реализовать улучшенную обработку ошибок API при создании и редактировании плейлистов:
- Различать типы ошибок (модерация, авторизация, сеть и т.д.)
- Предоставлять понятные сообщения пользователям
- Давать возможность повторить попытку при ошибке модерации
- Добавить предварительную валидацию названий
- Логировать ошибки модерации для анализа

## Этап 1: Улучшение обработки ошибок в `yandex_client_manager.py`

### Задачи

- [ ] Импортировать специфичные исключения из `yandex_music.exceptions`:
  - `BadRequestError` - ошибки 400 (некорректный запрос, модерация)
  - `ForbiddenError` - ошибки 403 (недостаточно прав, модерация)
  - `UnauthorizedError` - ошибки 401 (недействительный токен)
  - `TimedOutError` - таймауты
  - `NetworkError` - сетевые ошибки
  - `YandexMusicError` - базовое исключение

- [ ] Изменить сигнатуру `create_playlist()`:
  ```python
  async def create_playlist(self, telegram_id: Optional[int], title: str) -> Tuple[Optional[Dict], Optional[str]]:
      """
      Returns:
          Tuple[Optional[Dict], Optional[str]]: (результат, сообщение об ошибке)
          Если результат None, то error содержит понятное сообщение для пользователя
      """
  ```

- [ ] Добавить обработку различных типов исключений:
  - `BadRequestError` - проверять наличие ключевых слов "moderation", "name", "title", "invalid" в сообщении
  - `ForbiddenError` - недостаточно прав или модерация не пройдена
  - `UnauthorizedError` - проблема с токеном
  - `TimedOutError` - таймаут запроса
  - `NetworkError` - сетевые проблемы
  - `YandexMusicError` - другие ошибки API
  - `Exception` - неожиданные ошибки

- [ ] Добавить понятные сообщения об ошибках для пользователей:
  - Ошибка модерации: "Название плейлиста не прошло модерацию. Пожалуйста, используйте другое название."
  - Некорректный запрос: "Некорректный запрос. Проверьте название плейлиста."
  - Недостаточно прав: "Недостаточно прав для создания плейлиста или название не прошло модерацию."
  - Недействительный токен: "Токен Яндекс.Музыки недействителен. Используйте /set_token для обновления."
  - Таймаут: "Превышено время ожидания ответа. Попробуйте еще раз."
  - Сетевые ошибки: "Проблема с подключением к Яндекс.Музыке. Попробуйте позже."
  - Другие ошибки: "Ошибка при создании плейлиста. Попробуйте еще раз."

- [ ] Добавить логирование ошибок модерации:
  ```python
  if "модерац" in error_message.lower() or "moderation" in error_message.lower():
      logger.warning(
          f"Ошибка модерации названия плейлиста: title='{title}', "
          f"user_id={telegram_id}, error={str(e)}"
      )
  ```

### Связанные файлы

- `yandex_client_manager.py` - метод `create_playlist()`

## Этап 2: Улучшение обработки ошибок в `services/yandex_service.py`

### Задачи

- [ ] Импортировать специфичные исключения из `yandex_music.exceptions`

- [ ] Изменить метод `set_playlist_name()` для различения типов ошибок:
  ```python
  def set_playlist_name(
      self,
      playlist_kind: str,
      owner_id: str,
      new_name: str,
      max_retries: int = 2
  ) -> Tuple[bool, Optional[str]]:
      """
      Returns:
          Tuple[bool, Optional[str]]: (успех, понятное сообщение об ошибке)
      """
  ```

- [ ] Добавить обработку ошибок модерации:
  - `BadRequestError` - проверять наличие ключевых слов в сообщении
  - `ForbiddenError` - недостаточно прав или модерация
  - Сохранить обработку ошибок revision для повторных попыток

- [ ] Улучшить сообщения об ошибках:
  - Ошибка модерации: "Название плейлиста не прошло модерацию. Пожалуйста, используйте другое название."
  - Некорректное название: "Некорректное название плейлиста."
  - Недостаточно прав: "Недостаточно прав для изменения названия или название не прошло модерацию."

- [ ] Добавить логирование ошибок модерации

### Связанные файлы

- `services/yandex_service.py` - метод `set_playlist_name()`

## Этап 3: Улучшение обработчиков в боте

### Задачи

- [ ] Обновить `create_playlist_name()` в `handlers/commands.py`:
  - Изменить обработку результата `create_playlist()` (теперь возвращает кортеж)
  - Добавить проверку на ошибку модерации
  - При ошибке модерации - не очищать state, предложить ввести другое название
  - Для других ошибок - показывать сообщение и очищать state

- [ ] Обновить обработчик редактирования названия:
  - Найти обработчик редактирования названия (вероятно в `handlers/callbacks.py` или `handlers/messages.py`)
  - Добавить проверку на ошибку модерации
  - При ошибке модерации - возвращать в состояние редактирования
  - Для других ошибок - показывать сообщение

- [ ] Улучшить сообщения для пользователей:
  - Использовать понятные формулировки
  - Добавить эмодзи для лучшей читаемости
  - Предлагать конкретные действия

### Связанные файлы

- `handlers/commands.py` - метод `create_playlist_name()`
- `handlers/callbacks.py` или `handlers/messages.py` - обработчик редактирования названия
- `handlers/states.py` - состояния FSM (если нужно)

## Этап 4: Добавление предварительной валидации (опционально)

### Задачи

- [ ] Создать функцию `validate_playlist_name()` в `utils/`:
  - Проверка на пустое название
  - Проверка длины (максимум 100 символов)
  - Проверка на только пробелы
  - Проверка на несколько пробелов подряд
  - Проверка на недопустимые символы (разрешать: буквы, цифры, пробелы, дефисы, подчеркивания, скобки, кавычки, знаки препинания)

- [ ] Интегрировать валидацию в обработчики:
  - В `create_playlist_name()` - проверять название перед отправкой в API
  - В обработчике редактирования названия - проверять новое название

- [ ] Добавить понятные сообщения об ошибках валидации

### Связанные файлы

- `utils/` - новый файл для валидации (например, `utils/validation.py`)
- `handlers/commands.py` - использование валидации
- Обработчик редактирования названия

## Этап 5: Тестирование

### Задачи

- [ ] Протестировать создание плейлиста:
  - С нормальным названием (должно работать)
  - С длинным названием (должна быть валидация)
  - С недопустимыми символами (должна быть валидация)
  - С пустым названием (должна быть валидация)

- [ ] Протестировать обработку ошибок:
  - Ошибка модерации (если возможно безопасно воспроизвести)
  - Ошибка авторизации (недействительный токен)
  - Сетевые ошибки (можно симулировать)
  - Таймауты (можно симулировать)

- [ ] Протестировать редактирование названия:
  - С нормальным названием
  - С некорректным названием (должна быть валидация)
  - Обработка ошибок модерации

- [ ] Проверить логирование:
  - Ошибки модерации логируются отдельно
  - Логи содержат полезную информацию (title, user_id, error)

## Чеклист выполнения

### Этап 1: Улучшение обработки ошибок в `yandex_client_manager.py`
- [ ] Импортированы специфичные исключения
- [ ] Изменена сигнатура `create_playlist()`
- [ ] Добавлена обработка различных типов исключений
- [ ] Добавлены понятные сообщения об ошибках
- [ ] Добавлено логирование ошибок модерации

### Этап 2: Улучшение обработки ошибок в `services/yandex_service.py`
- [ ] Импортированы специфичные исключения
- [ ] Изменен метод `set_playlist_name()`
- [ ] Добавлена обработка ошибок модерации
- [ ] Улучшены сообщения об ошибках
- [ ] Добавлено логирование ошибок модерации

### Этап 3: Улучшение обработчиков в боте
- [ ] Обновлен `create_playlist_name()`
- [ ] Обновлен обработчик редактирования названия
- [ ] Улучшены сообщения для пользователей

### Этап 4: Добавление предварительной валидации
- [ ] Создана функция `validate_playlist_name()`
- [ ] Интегрирована валидация в обработчики
- [ ] Добавлены сообщения об ошибках валидации

### Этап 5: Тестирование
- [ ] Протестировано создание плейлиста
- [ ] Протестирована обработка ошибок
- [ ] Протестировано редактирование названия
- [ ] Проверено логирование

## Приоритет

Высокий - критично для безопасности аккаунта и улучшения пользовательского опыта.

## Связанные файлы

- `yandex_client_manager.py` - создание плейлистов
- `services/yandex_service.py` - редактирование названий
- `handlers/commands.py` - обработка создания плейлиста
- `handlers/callbacks.py` или `handlers/messages.py` - обработка редактирования названия
- `utils/` - валидация названий (новый файл)
- `docs/research/playlist_moderation_research.md` - исследование

## Примечания

- При тестировании ошибок модерации нужно быть осторожным, чтобы не нарушить правила Яндекс.Музыки
- Формат ошибок API может отличаться от ожидаемого - нужно быть готовым к адаптации
- Список запрещенных слов/символов неизвестен и может изменяться
- Предварительная валидация может только уменьшить вероятность ошибок, но не гарантировать успех

