# Миграция треков между музыкальными сервисами

**Статус**: 📋 Запланировано  
**Приоритет**: 🟡 Средний  
**Категория**: Новые функции

## Цель

Предоставить пользователям возможность переносить плейлисты и треки из одного музыкального сервиса в другой (например, Apple Music → Яндекс.Музыка, Яндекс.Музыка → Apple Music и т.д.).

## Концепция

Использование существующей функциональности бота для создания плейлиста в целевом сервисе и добавления треков из исходного сервиса.

## Сценарий использования

1. Пользователь отправляет ссылку на плейлист из одного сервиса (например, Apple Music)
2. Бот создает новый плейлист в целевом сервисе (например, Яндекс.Музыка)
3. Бот добавляет все треки из исходного плейлиста в новый плейлист целевого сервиса
4. Пользователь получает ссылку на созданный плейлист в целевом сервисе с сохраненным порядком треков

## Требования

- Сохранение позиционирования треков (порядок треков в исходном плейлисте должен сохраняться в целевом)
- Поддержка миграции между любыми интегрированными сервисами (Яндекс.Музыка, Apple Music, Spotify, ВК-музыка и т.д.)
- Обработка случаев, когда трек недоступен в целевом сервисе (пропуск с уведомлением или поиск альтернативы)
- Сохранение метаданных плейлиста (название, описание, обложка, если возможно)

## Задачи

- [ ] Расширить `LinkParser` для определения источника плейлиста (какой сервис)
- [ ] Создать метод миграции в сервисах (например, `migrate_playlist` в `PlaylistService`)
- [ ] Реализовать логику последовательного добавления треков с сохранением порядка
- [ ] Добавить команду/кнопку для запуска миграции (например, `/migrate_playlist` или автоматически при отправке ссылки на плейлист другого сервиса)
- [ ] Показывать прогресс миграции для больших плейлистов
- [ ] Предоставлять отчет о результатах миграции (сколько треков успешно добавлено, сколько пропущено)
- [ ] Обработать случаи, когда трек не найден в целевом сервисе:
  - Пропустить трек с уведомлением
  - Попытаться найти альтернативу (поиск по названию и исполнителю)

## UX

- Интерактивный диалог для выбора целевого сервиса (если у пользователя несколько активных плейлистов в разных сервисах)
- Возможность выбора названия для нового плейлиста (по умолчанию - название исходного плейлиста)
- Уведомления о проблемах при миграции (трек не найден, ошибка API и т.д.)
- Ссылка на созданный плейлист в целевом сервисе после завершения миграции
- Прогресс-бар или периодические обновления статуса для больших плейлистов

## Реализация

### Алгоритм миграции

1. Парсинг ссылки на исходный плейлист
2. Получение списка треков из исходного сервиса
3. Создание нового плейлиста в целевом сервисе
4. Последовательное добавление треков с сохранением порядка:
   - Поиск трека в целевом сервисе
   - Добавление трека в плейлист
   - Обработка ошибок (трек не найден, ошибка API)
5. Формирование отчета о результатах

## Приоритет

Средний - важная фича для удобства пользователей, может быть реализована после базовой интеграции других сервисов.

## Зависимости

Требует реализации task-feature-music-services-integration.md для поддержки множественных сервисов.

## Связанные файлы

- `services/link_parser.py` - парсинг ссылок
- `services/playlist_service.py` - метод миграции
- `services/yandex_service.py`, `services/vk_music_service.py`, `services/spotify_service.py` - работа с сервисами
- `handlers/messages.py` - обработка команды миграции

