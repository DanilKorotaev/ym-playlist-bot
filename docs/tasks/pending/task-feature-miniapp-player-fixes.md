# Задачи по доработке Mini App плеера

**Статус**: 📝 Запланировано  
**Приоритет**: 🟡 Средний  
**Категория**: Улучшения  
**Связанная задача**: `task-feature-miniapp-player.md`

## Описание

Задачи по доработке и улучшению Mini App плеера после завершения этапа 3 (REST API).

## Задачи

### 1. Исправление воспроизведения треков

**Проблема:** Ошибка "Failed to build streaming URL" при попытке воспроизвести трек.

**Что нужно сделать:**
- [x] Исследовать причину ошибки в методе `_build_streaming_url()` в `miniapp/api/routes.py`
- [x] Проверить логику получения `download_info` из API Яндекс.Музыки
- [x] Убедиться, что правильно обрабатываются все форматы ответа (XML, прямые URL, вложенные объекты)
- [x] Добавить более детальную обработку ошибок с понятными сообщениями
- [x] Протестировать на разных типах треков ✅ **Проверено: треки воспроизводятся корректно**

**Выполнено:**
- ✅ Переработан метод `_build_streaming_url()` для обработки списка вариантов `download_info`
- ✅ Добавлена обработка случая, когда `download_info` - это список (выбирается первый вариант)
- ✅ Улучшена обработка XML в разных местах (host, path, результат get_direct_link())
- ✅ Добавлена функция `_extract_url_from_xml()` для извлечения URL из XML
- ✅ Улучшено логирование на всех этапах построения URL
- ✅ Улучшена обработка ошибок в `get_track_stream_url()` с детальными сообщениями
- ✅ Добавлена проверка всех возможных форматов ответа (прямые URL, XML, вложенные объекты)

**Файлы:**
- `miniapp/api/routes.py` - метод `_build_streaming_url()` и `get_track_stream_url()`
- `tools/test_download_tracks.py` - примеры работы с download_info

### 2. Тестирование работы плеера

**Что нужно сделать:**
- [ ] Протестировать воспроизведение треков после исправления ошибки
- [ ] Проверить работу на разных платформах (Android, iOS, Desktop, Web)
- [ ] Проверить работу с разными типами плейлистов (личные, общие)
- [ ] Проверить обработку ошибок (недоступные треки, проблемы с сетью)
- [ ] Проверить работу прогресс-бара и перемотки
- [ ] Проверить работу кнопок управления (play/pause, prev/next)

**Результат:** Полное описание работы плеера и найденных проблем.

### 3. Рефакторинг miniapp/api/routes.py

**Задача:** Проанализировать и улучшить структуру создания зависимостей (БД, client_manager).

**Текущая реализация:**
- Используется фабричный паттерн для создания новых экземпляров БД и client_manager
- Для PostgreSQL создаются отдельные экземпляры с новыми connection pools
- Для SQLite используется общий экземпляр с блокировкой

**Что нужно сделать:**
- [ ] Проанализировать текущую реализацию создания зависимостей
- [ ] Изучить паттерны создания зависимостей в FastAPI (Dependency Injection)
- [ ] Предложить улучшенную архитектуру:
  - Возможно, использовать FastAPI Depends с фабриками
  - Улучшить управление жизненным циклом зависимостей
  - Упростить код инициализации
- [ ] Задокументировать выбранный подход
- [ ] Реализовать улучшения

**Файлы:**
- `miniapp/api/routes.py` - текущая реализация
- `database/postgresql_db.py` - создание connection pools
- `database/sqlite_db.py` - блокировки для многопоточности

**Заметки:**
- Текущая реализация работает, но можно улучшить
- Нужно учесть различия между SQLite и PostgreSQL
- Важно сохранить правильную работу с event loops

### 4. Улучшение размещения кнопки Web App

**Проблема:** Кнопка Web App в ReplyKeyboard вызывает ошибки при открытии в Telegram Desktop.

**Текущее состояние:**
- Кнопка в ReplyKeyboard вызывает проблемы с initData в Telegram Desktop
- Inline-кнопка работает корректно, но размещена под сообщением

**Что нужно сделать:**
- [ ] Убрать кнопку Web App из ReplyKeyboard (временно)
- [ ] Оставить только inline-кнопку
- [ ] Исследовать варианты размещения:
  - В главном меню (`/start`, `/main_menu`) - уже есть
  - В списке плейлистов (кнопка "Открыть плеер" для каждого плейлиста)
  - В информации о плейлисте (кнопка "Открыть в плеере")
  - В отдельной команде `/player` или `/open_player`
- [ ] Выбрать наиболее интуитивный вариант
- [ ] Реализовать выбранный вариант
- [ ] Протестировать на разных платформах

**Варианты размещения:**

1. **В команде `/player`** - отдельная команда для открытия плеера
   - Плюсы: Явная команда, легко найти
   - Минусы: Нужно помнить команду

2. **В списке плейлистов** - кнопка "🎵 Открыть в плеере" для каждого плейлиста
   - Плюсы: Контекстно, понятно для какого плейлиста
   - Минусы: Много кнопок, может быть перегружено

3. **В информации о плейлисте** - кнопка при просмотре плейлиста
   - Плюсы: Логично, когда пользователь уже смотрит плейлист
   - Минусы: Нужно сначала открыть плейлист

4. **В главном меню (inline-кнопка)** - текущий вариант
   - Плюсы: Всегда доступна
   - Минусы: Может быть неочевидна

**Рекомендация:** Комбинация вариантов 1 и 3:
- Команда `/player` для быстрого доступа
- Кнопка в информации о плейлисте для контекстного открытия

**Файлы:**
- `handlers/keyboards.py` - убрать кнопку из ReplyKeyboard
- `handlers/commands.py` - добавить команду `/player` (если выбран вариант)
- `handlers/callbacks.py` - добавить кнопку в информацию о плейлисте (если выбран вариант)

## Приоритеты

1. **Высокий:** Исправление воспроизведения треков (#1)
2. **Средний:** Тестирование (#2)
3. **Средний:** Улучшение размещения кнопки (#4)
4. **Низкий:** Рефакторинг (#3) - можно отложить, текущая реализация работает

## Связанные файлы

- `miniapp/api/routes.py` - REST API эндпоинты
- `miniapp/static/js/player.js` - логика плеера
- `miniapp/static/js/app.js` - основная логика Mini App
- `handlers/keyboards.py` - клавиатуры бота
- `handlers/commands.py` - команды бота
- `tools/test_download_tracks.py` - примеры работы с download_info

