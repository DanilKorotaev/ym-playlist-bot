# Задачи по доработке Mini App плеера

**Статус**: ✅ Выполнено  
**Приоритет**: 🟡 Средний  
**Категория**: Улучшения  
**Связанная задача**: `task-feature-miniapp-player.md`

## Описание

Задачи по доработке и улучшению Mini App плеера после завершения этапа 3 (REST API).

## Задачи

### 1. Исправление воспроизведения треков

**Проблема:** Ошибка "Failed to build streaming URL" при попытке воспроизвести трек.

**Что нужно сделать:**
- [x] Исследовать причину ошибки в методе `_build_streaming_url()` в `miniapp/api/routes.py`
- [x] Проверить логику получения `download_info` из API Яндекс.Музыки
- [x] Убедиться, что правильно обрабатываются все форматы ответа (XML, прямые URL, вложенные объекты)
- [x] Добавить более детальную обработку ошибок с понятными сообщениями
- [x] Протестировать на разных типах треков ✅ **Проверено: треки воспроизводятся корректно**

**Выполнено:**
- ✅ Переработан метод `_build_streaming_url()` для обработки списка вариантов `download_info`
- ✅ Добавлена обработка случая, когда `download_info` - это список (выбирается первый вариант)
- ✅ Улучшена обработка XML в разных местах (host, path, результат get_direct_link())
- ✅ Добавлена функция `_extract_url_from_xml()` для извлечения URL из XML
- ✅ Улучшено логирование на всех этапах построения URL
- ✅ Улучшена обработка ошибок в `get_track_stream_url()` с детальными сообщениями
- ✅ Добавлена проверка всех возможных форматов ответа (прямые URL, XML, вложенные объекты)

**Файлы:**
- `miniapp/api/routes.py` - метод `_build_streaming_url()` и `get_track_stream_url()`
- `tools/test_download_tracks.py` - примеры работы с download_info

### 2. Рефакторинг miniapp/api/routes.py

**Задача:** Проанализировать и улучшить структуру создания зависимостей (БД, client_manager).

**Статус:** ✅ Выполнено

**Что было сделано:**
- [x] Проанализирована текущая реализация создания зависимостей
- [x] Изучены паттерны создания зависимостей в FastAPI (Dependency Injection)
- [x] Реализована улучшенная архитектура:
  - Использованы FastAPI Depends для dependency injection
  - Использован app.state для хранения зависимостей
  - Добавлены lifespan events для управления жизненным циклом
  - Упрощен код инициализации (убраны глобальные переменные)
- [x] Задокументирован выбранный подход (в коде через docstrings)

**Реализованные улучшения:**

1. **Убраны глобальные переменные** - все зависимости хранятся в `app.state`
2. **Использованы FastAPI Depends** - чистый dependency injection через функции `get_db()`, `get_client_manager()`, `get_playlist_service()`
3. **Добавлены lifespan events** - инициализация и очистка зависимостей происходит в `lifespan()` функции в `server.py`
4. **Упрощена логика создания зависимостей**:
   - Для PostgreSQL: создается отдельный экземпляр с новым connection pool в lifespan
   - Для SQLite: используется общий экземпляр (с блокировкой)
5. **Улучшена читаемость кода** - зависимости явно указаны в сигнатурах функций роутов

**Файлы:**
- `miniapp/api/routes.py` - рефакторинг dependency injection
- `miniapp/api/server.py` - добавлены lifespan events
- `miniapp/api/main.py` - обновлена инициализация

**Архитектура:**
- Зависимости инициализируются в `lifespan()` при старте FastAPI
- Зависимости хранятся в `app.state.db` и `app.state.client_manager`
- Dependency functions (`get_db()`, `get_client_manager()`) получают зависимости из `app.state` через `Request`
- Все роуты используют `Depends()` для получения зависимостей

### 3. Улучшение размещения кнопки Web App

**Проблема:** Кнопка Web App в ReplyKeyboard вызывает ошибки при открытии в Telegram Desktop.

**Текущее состояние:**
- ✅ Кнопка в ReplyKeyboard убрана из-за проблем с initData в Telegram Desktop
- ✅ Inline-кнопка работает корректно

**Что нужно сделать:**
- [x] Убрать кнопку Web App из ReplyKeyboard (временно) ✅ **Выполнено**
- [x] Оставить только inline-кнопку ✅ **Выполнено**
- [x] Исследовать варианты размещения ✅ **Выполнено**
- [x] Выбрать наиболее интуитивный вариант ✅ **Выбрана комбинация вариантов 1 и 3**
- [x] Реализовать выбранный вариант ✅ **Выполнено**
- [ ] Протестировать на разных платформах

**Реализованные варианты размещения:**

1. ✅ **Команда `/player`** - отдельная команда для открытия плеера
   - Реализовано: добавлена команда `/player` в `handlers/commands.py`
   - Регистрация в `bot.py`
   - Показывает inline-кнопку с текстом "🎵 Открыть плеер"

2. ✅ **В информации о плейлисте** - кнопка "🎵 Открыть в плеере" при просмотре плейлиста
   - Реализовано: добавлена кнопка в `playlist_info()` в `handlers/commands.py`
   - Кнопка показывается для всех пользователей с доступом к плейлисту
   - Размещена в начале списка кнопок для лучшей видимости

3. ✅ **В меню редактирования плейлиста** - кнопка "🎵 Открыть в плеере"
   - Реализовано: добавлена кнопка в `_create_edit_playlist_keyboard()` в `handlers/callbacks.py`
   - Кнопка показывается в начале меню редактирования

4. ✅ **В главном меню (inline-кнопка)** - текущий вариант сохранен
   - Оставлена inline-кнопка в `/start` и `/main_menu` для обратной совместимости

**Улучшения:**
- ✅ Улучшена функция `get_miniapp_inline_keyboard()` - добавлен параметр `button_text` для кастомизации текста кнопки
- ✅ Кнопки размещены в логичных местах для лучшего UX

**Файлы:**
- ✅ `handlers/keyboards.py` - улучшена функция `get_miniapp_inline_keyboard()` с поддержкой кастомного текста
- ✅ `handlers/commands.py` - добавлена команда `/player` и кнопка в `playlist_info()`
- ✅ `handlers/callbacks.py` - добавлена кнопка в меню редактирования плейлиста
- ✅ `bot.py` - зарегистрирована команда `/player`

## Приоритеты

1. **Высокий:** Исправление воспроизведения треков (#1) ✅ **Выполнено**
2. **Средний:** Рефакторинг (#2) ✅ **Выполнено**
3. **Средний:** Улучшение размещения кнопки (#3) ✅ **Выполнено**

### 4. Исправления отображения и функциональности плеера

**Проблемы:**
- Отображался ID трека вместо названия
- Кнопка паузы показывала песочные часы и не работала
- Кнопки на нативном плеере (iOS) перематывали трек вместо переключения
- Иконки выглядели по-разному на Mac и iPhone (эмодзи)

**Что было сделано:**
- [x] Исправлено отображение названия трека в `routes.py` - теперь извлекается `title` из объекта трека
- [x] Исправлена логика кнопки паузы - добавлена обработка события `canplaythrough` для сброса состояния загрузки
- [x] Реализован Media Session API для нативного плеера:
  - Инициализация при старте приложения
  - Отключение перемотки (`seekbackward`/`seekforward` = null)
  - Установка обработчиков `previoustrack` и `nexttrack` для переключения треков
  - Обновление метаданных трека (название, артисты, обложка)
- [x] Унифицированы иконки - заменены эмодзи на простые Unicode символы:
  - Предыдущий: `⏮` → `◄◄`
  - Следующий: `⏭` → `►►`
  - Play/Pause: `▶` и `⏸` (остались без изменений)
- [x] Добавлена обложка трека в API ответ (из альбома)
- [x] Добавлены кнопки play/pause в список треков для каждого трека

**Файлы:**
- `miniapp/api/routes.py` - исправлено извлечение названия трека, добавлена обложка
- `miniapp/static/js/app.js` - исправлена логика паузы, реализован Media Session API
- `miniapp/static/js/player.js` - добавлена обработка события `canplaythrough`
- `miniapp/static/index.html` - заменены иконки на простые символы
- `miniapp/static/css/styles.css` - улучшен стиль списка треков с кнопками

## Статус

**Все задачи выполнены.** Задача готова к закрытию.

## Связанные файлы

- `miniapp/api/routes.py` - REST API эндпоинты
- `miniapp/static/js/player.js` - логика плеера
- `miniapp/static/js/app.js` - основная логика Mini App
- `handlers/keyboards.py` - клавиатуры бота
- `handlers/commands.py` - команды бота
- `tools/test_download_tracks.py` - примеры работы с download_info

