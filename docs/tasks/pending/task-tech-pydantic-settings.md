# Добавить pydantic-based settings для валидации конфигурации

**Статус**: 📋 Запланировано  
**Приоритет**: 🟡 Средний  
**Категория**: Технические улучшения

## Описание

Заменить использование переменных окружения через `os.getenv()` на pydantic-based settings для валидации конфигурации и улучшения типизации.

## Задачи

- [ ] Исследовать библиотеки для работы с settings (pydantic-settings)
- [ ] Создать класс Settings с валидацией всех переменных окружения
- [ ] Определить обязательные и опциональные параметры
- [ ] Добавить валидацию типов и значений
- [ ] Обновить код для использования нового класса Settings
- [ ] Добавить обработку ошибок при отсутствии обязательных параметров
- [ ] Документировать все параметры конфигурации

## Реализация

### Структура Settings

```python
class Settings(BaseSettings):
    # Telegram
    telegram_token: str
    
    # Database
    database_url: str
    
    # Yandex Music
    yandex_token: Optional[str] = None
    
    # Limits
    playlist_limit: int = 2
    
    # Admin
    admin_ids: List[int] = []
    
    class Config:
        env_file = ".env"
        env_file_encoding = "utf-8"
```

### Преимущества

- Автоматическая валидация типов
- Проверка обязательных параметров при старте
- Улучшенная типизация в коде
- Централизованное управление конфигурацией
- Документация параметров через аннотации типов

## Связанные файлы

- `bot.py` - использование settings
- Все файлы, использующие `os.getenv()`

