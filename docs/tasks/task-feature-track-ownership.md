# Управление треками по авторству

**Статус**: 📋 Запланировано  
**Приоритет**: 🟡 Средний  
**Категория**: Новые функции

## Описание

Добавить возможность удалять треки в плейлисте тем, кто их добавил, для улучшения контроля над содержимым плейлиста.

## Функциональность

- Возможность удалять треки в плейлисте тем, кто их добавил
- Проверка прав: пользователь может удалять только свои треки (или создатель плейлиста может удалять любые)
- Хранение информации о том, кто добавил каждый трек

## Задачи

- [ ] Расширить структуру БД для хранения информации об авторстве треков
  - Расширить таблицу `actions` или создать новую таблицу `track_contributions`
  - Хранить `user_id`, `playlist_id`, `track_id`, `added_at`
- [ ] Обновить логику добавления треков для сохранения информации об авторстве
- [ ] Обновить логику удаления в `PlaylistService.delete_track` для проверки авторства
- [ ] Реализовать проверку прав:
  - Пользователь может удалять только свои треки
  - Создатель плейлиста может удалять любые треки
- [ ] Обновить интерфейс удаления треков для отображения авторства
- [ ] Добавить информацию об авторстве в список треков

## Реализация

### Структура БД

```sql
CREATE TABLE track_contributions (
    id SERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    playlist_id INTEGER NOT NULL,
    track_id VARCHAR NOT NULL,
    added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (playlist_id) REFERENCES playlists(id)
);
```

### Логика проверки прав

- При удалении трека проверять:
  1. Является ли пользователь создателем плейлиста? → разрешить удаление
  2. Является ли пользователь автором трека? → разрешить удаление
  3. Иначе → запретить удаление

## UX

- Показывать, кто добавил каждый трек в списке треков
- При попытке удалить чужой трек показывать сообщение: "Вы можете удалять только треки, которые вы добавили"
- Создатель плейлиста видит все треки и может удалять любые

## Связанные файлы

- `database/` - структура БД и миграции
- `services/playlist_service.py` - логика удаления треков
- `handlers/callbacks.py` - обработка удаления треков

