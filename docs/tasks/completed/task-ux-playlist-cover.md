# Прикрепление картинки из плейлиста ЯМ в сообщение с инфо о плейлисте

**Статус**: ✅ Выполнено  
**Приоритет**: 🟡 Средний  
**Категория**: Улучшения UX

## Описание

При отображении информации о плейлисте (`/playlist_info`) прикреплять обложку плейлиста из Яндекс.Музыки, чтобы сделать интерфейс более визуально привлекательным и информативным.

## Задачи

- [x] Исследовать, как получить обложку плейлиста из API Яндекс.Музыки
- [x] Определить формат и размер обложки для Telegram
- [x] Реализовать загрузку и отправку обложки вместе с информацией о плейлисте
- [x] Обработать случаи, когда обложка недоступна
- [x] Обновить команду `/playlist_info`

## Реализация

### UX

- При выполнении `/playlist_info` отправлять фото обложки плейлиста
- Под фото размещать текст с информацией о плейлисте
- Если обложка недоступна, отправлять только текст (как сейчас)

### Технические детали

- Расширить `YandexService` для получения обложки плейлиста
- Использовать `bot.send_photo()` вместо `bot.send_message()`
- Кэшировать обложки для уменьшения количества запросов к API
- Обработать ошибки при загрузке обложки

## Реализовано

### Изменения в коде

1. **`services/yandex_service.py`**:
   - Добавлен метод `download_playlist_cover()` для скачивания обложки по URL с авторизацией
   - Метод использует заголовки авторизации из клиента Яндекс.Музыки

2. **`services/playlist_service.py`**:
   - Обновлен метод `get_playlist_cover_url()` - добавлен параметр `only_custom` для получения любой обложки (не только пользовательской)
   - Добавлен метод `get_playlist_cover_image()` для получения изображения обложки в виде байтов

3. **`handlers/commands.py`**:
   - Обновлен метод `playlist_info()` для отправки фото с текстом
   - Если обложка доступна, используется `message.answer_photo()` с `caption`
   - Если обложка недоступна, отправляется только текст (как раньше)
   - Добавлен импорт `InputFile` и `io` для работы с изображениями

### Особенности реализации

- Обложка скачивается с авторизацией через заголовки клиента Яндекс.Музыки
- Поддерживаются как пользовательские обложки (custom), так и автоматические (мозаика из треков)
- Обработка ошибок: если обложка недоступна или не найдена, отправляется только текст
- Изображение передается через `InputFile` из байтов в памяти

## Связанные файлы

- `services/yandex_service.py` - получение обложки плейлиста
- `services/playlist_service.py` - бизнес-логика получения обложки
- `handlers/commands.py` - команда `/playlist_info`

