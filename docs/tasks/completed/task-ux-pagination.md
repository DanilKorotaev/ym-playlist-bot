# Улучшение отображения списка треков (пагинация)

**Статус**: ✅ Выполнено  
**Приоритет**: 🟡 Средний  
**Категория**: Улучшения UX

## Описание

Для больших плейлистов необходимо реализовать пагинацию при отображении списка треков, чтобы не перегружать пользователя большим количеством информации в одном сообщении.

## Задачи

- [x] Исследовать возможности Telegram Bot API для пагинации
- [x] Определить оптимальный размер страницы (количество треков на странице) - установлено 12 треков на страницу
- [x] Реализовать навигацию по страницам (кнопки "Вперед"/"Назад")
- [x] Обновить команду/кнопку для просмотра списка треков
- [x] Добавить отображение текущей страницы и общего количества страниц

## Реализация

### UX

- ✅ Показывать по 12 треков на странице (константа `TRACKS_PER_PAGE`)
- ✅ Добавить inline-кнопки для навигации: "◀️ Назад", "Вперед ▶️"
- ✅ Отображать информацию: "📄 Страница 1 из 5"
- Состояние пагинации передается через callback_data (playlist_id и номер страницы)

### Технические детали

- ✅ Использовать callback_data для передачи номера страницы (формат: `list_page_{playlist_id}_{page}`)
- Список треков получается при каждом переключении страницы (актуальные данные)
- ✅ Обновлять сообщение вместо отправки нового при переключении страниц (через `edit_text`)

### Реализованные изменения

1. **handlers/commands.py**:
   - Добавлена константа `TRACKS_PER_PAGE = 12`
   - Добавлен метод `_format_tracks_page()` для форматирования страницы треков
   - Обновлен метод `show_list()` для отображения первой страницы с кнопками навигации

2. **handlers/callbacks.py**:
   - Добавлена поддержка `client_manager` и `playlist_service` в конструкторе
   - Добавлен обработчик `_handle_list_page()` для навигации по страницам
   - Добавлен метод `_format_tracks_page()` (дублирование логики для независимости)
   - Добавлена обработка callback `list_page_{playlist_id}_{page}` в роутере

3. **bot.py**:
   - Обновлена инициализация `CallbackHandlers` для передачи `client_manager`

## Связанные файлы

- `handlers/callbacks.py` - обработка навигации по страницам
- `handlers/commands.py` - команда просмотра треков
- `services/playlist_service.py` - получение списка треков

