# Улучшение ссылок на плейлисты

**Статус**: ✅ Реализовано  
**Приоритет**: 🟡 Средний  
**Категория**: Новые функции

## Описание

Изменить формат ссылок на плейлисты с `https://music.yandex.ru/users/{owner_id}/playlists/{playlist_kind}` на более короткий формат `https://music.yandex.ru/playlists/{uuid}`.

## Текущий формат

```
https://music.yandex.ru/users/{owner_id}/playlists/{playlist_kind}
```

## Желаемый формат

```
https://music.yandex.ru/playlists/{uuid}
```

## Задачи

- [x] Изучить API Яндекс.Музыки для получения UUID плейлиста
- [x] Проверить, доступен ли UUID через текущий API
- [x] Обновить метод `PlaylistService.get_yandex_link` для использования UUID формата
- [ ] Протестировать новые ссылки на работоспособность
- [x] Обновить все места, где генерируются ссылки на плейлисты

## Реализация

### Исследование API

- Проверить, возвращает ли API UUID плейлиста при создании/получении
- Если UUID недоступен, найти альтернативный способ получения короткой ссылки
- Проверить формат ссылок в документации API

### Обновление кода

- Обновить `PlaylistService.get_yandex_link()` для использования UUID
- Сохранять UUID в БД при создании плейлиста (если необходимо)
- Обновить все места использования ссылок

## Преимущества

- Более короткие и удобные ссылки
- Более универсальный формат (не привязан к owner_id)
- Лучшая читаемость ссылок

## Связанные файлы

- `services/playlist_service.py` - метод `get_yandex_link()`
- `database/` - структура БД (возможно, нужно добавить поле uuid)

