# Процесс создания релизов

## Дата создания
2025-12-06

## Обзор

Данная документация описывает процесс создания релизов для проекта Liza Party Bot. Релизы позволяют:
- Отмечать стабильные версии кода
- Отслеживать изменения между версиями
- Контролировать, какая версия развернута на продакшене
- Упрощать откат к предыдущим версиям

---

## 1. Семантическое версионирование

Проект использует [Semantic Versioning](https://semver.org/lang/ru/) (SemVer):

**Формат версии:** `MAJOR.MINOR.PATCH`

- **MAJOR** (старшая версия) — увеличивается при несовместимых изменениях API
- **MINOR** (средняя версия) — увеличивается при добавлении новой функциональности с обратной совместимостью
- **PATCH** (младшая версия) — увеличивается при исправлении ошибок с обратной совместимостью

**Примеры:**
- `4.0.0` — первый релиз версии 4.0
- `4.1.0` — добавлена новая функция
- `4.1.1` — исправлена ошибка
- `5.0.0` — несовместимые изменения

---

## 2. Подготовка к релизу

### 2.1. Создание первого релиза

Если вы еще не создавали релизов, начните с версии `4.0.0` (или другой подходящей версии):

1. Обновите `CHANGELOG.md`:
   - Переместите все записи из `[Unreleased]` в раздел `[4.0.0]`
   - Добавьте дату релиза
   - Добавьте ссылки на сравнение версий (см. пример ниже)

2. Обновите `README.md` с версией `4.0.0`

3. Создайте тег: `git tag -a v4.0.0 -m "Release version 4.0.0"`

4. Отправьте тег: `git push origin v4.0.0`

5. Создайте релиз на GitHub через веб-интерфейс

### 2.2. Обновление CHANGELOG.md для последующих релизов

Перед созданием нового релиза обновите файл `CHANGELOG.md`:

1. Переместите записи из раздела `[Unreleased]` в новый раздел с версией
2. Добавьте дату релиза
3. Укажите ссылки на сравнение версий

### 2.3. Обновление версии в README.md

Обновите версию в файле `README.md`:

```markdown
> **Версия**: 4.1.0
```

### 2.3. Проверка кода

Перед созданием релиза убедитесь, что:
- ✅ Все изменения закоммичены
- ✅ Код протестирован
- ✅ CHANGELOG.md обновлен
- ✅ README.md обновлен (версия)
- ✅ Нет незавершенных задач в текущей версии

**Для первого релиза:**
- ✅ Все основные функции реализованы и протестированы
- ✅ Документация актуальна
- ✅ CI/CD настроен и работает

---

## 3. Создание релиза

### 3.1. Создание тега

**Вариант 1: Аннотированный тег (рекомендуется)**

```bash
# Создание аннотированного тега с сообщением
git tag -a v4.1.0 -m "Release version 4.1.0

- Добавлена новая функция X
- Улучшение Y
- Исправлена ошибка Z"

# Отправка тега в репозиторий
git push origin v4.1.0
```

**Вариант 2: Легкий тег**

```bash
# Создание легкого тега
git tag v4.1.0

# Отправка тега в репозиторий
git push origin v4.1.0
```

**Отправка всех тегов:**

```bash
git push origin --tags
```

### 3.2. Создание релиза на GitHub

1. Откройте репозиторий на GitHub
2. Перейдите в **Releases** → **Draft a new release**
3. Выберите созданный тег (например, `v4.1.0`)
4. Заполните заголовок: `v4.1.0` или `Release 4.1.0`
5. Скопируйте содержимое из `CHANGELOG.md` для этой версии в описание релиза
6. Нажмите **Publish release**

**Пример описания релиза:**

```markdown
## Что нового в версии 4.1.0

### Добавлено
- Новая функция X
- Улучшение Y

### Исправлено
- Исправлена ошибка Z

### Полный список изменений
См. [CHANGELOG.md](https://github.com/your-username/Liza_patry_bot/blob/main/CHANGELOG.md)
```

---

## 4. Автоматическое развертывание релизов

### 4.1. Настройка workflow для релизов

Если вы хотите, чтобы релизы автоматически развертывались на продакшен, обновите `.github/workflows/deploy.yml`:

```yaml
on:
  push:
    branches:
      - main
    tags:
      - 'v*'  # Развертывание при создании тега v1.0.0, v4.1.0 и т.д.
  workflow_dispatch:
```

**Важно:** При создании тега workflow автоматически запустится и развернет релиз на сервере.

### 4.2. Развертывание только релизов (без автоматического деплоя из main)

Если вы хотите развертывать только релизы (не каждый коммит в main), используйте отдельный workflow:

```yaml
# .github/workflows/release-deploy.yml
name: Deploy Release

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    # ... аналогично deploy.yml
```

---

## 5. Процесс работы с релизами

> **Примечание**: Проект использует Git Flow. Подробнее см. [Git Flow - Процесс разработки и релизов](git_flow.md).

### 5.1. Стандартный процесс (с Git Flow)

1. **Разработка** — работа в `feature/*` ветках от `develop`
2. **Мерж в develop** — завершение фич мержится в `develop`
3. **Создание release-ветки** — `./scripts/git_flow_release.sh start 4.1.0`
4. **Подготовка релиза** — обновление CHANGELOG.md и README.md в release-ветке
5. **Завершение release** — `./scripts/git_flow_release.sh finish 4.1.0` (создает тег и мержит в main и develop)
6. **Создание релиза на GitHub** — через веб-интерфейс
7. **Автоматическое развертывание** — workflow развернет релиз на сервере при создании тега

### 5.2. Hotfix процесс (с Git Flow)

Для срочных исправлений в продакшене:

1. Создайте hotfix-ветку: `./scripts/git_flow_hotfix.sh start 4.1.1`
2. Внесите исправления
3. Обновите CHANGELOG.md и README.md
4. Завершите hotfix: `./scripts/git_flow_hotfix.sh finish 4.1.1` (создает тег и мержит в main и develop)
5. Создайте релиз на GitHub через веб-интерфейс
6. Автоматическое развертывание запустится при создании тега

Для работы без Git Flow:

1. **Разработка** — работа в ветке `main` или feature-ветках
2. **Тестирование** — тестирование изменений
3. **Подготовка релиза** — обновление CHANGELOG.md и README.md
4. **Создание тега** — `git tag -a v4.1.0 -m "Release 4.1.0"`
5. **Отправка тега** — `git push origin v4.1.0`
6. **Создание релиза на GitHub** — через веб-интерфейс
7. **Автоматическое развертывание** — workflow развернет релиз на сервере

---

## 6. Откат к предыдущему релизу

### 6.1. Откат через тег

```bash
# На сервере
cd /path/to/project
git fetch origin --tags
git checkout v4.0.0  # Откат к версии 4.0.0
docker compose build bot
docker compose stop bot
docker compose up -d bot
```

### 6.2. Откат через GitHub Releases

1. Откройте GitHub → **Releases**
2. Найдите нужный релиз
3. Скопируйте хеш коммита или тег
4. Выполните откат на сервере (см. выше)

---

## 7. Best Practices

### 7.1. Частота релизов

- **PATCH** (4.1.0 → 4.1.1) — при исправлении ошибок, можно часто
- **MINOR** (4.0.0 → 4.1.0) — при добавлении функций, раз в 1-2 недели
- **MAJOR** (4.0.0 → 5.0.0) — при крупных изменениях, редко

### 7.2. Сообщения коммитов

Используйте понятные сообщения коммитов:
- ✅ `feat: добавлена функция создания плейлиста`
- ✅ `fix: исправлена ошибка парсинга ссылок`
- ✅ `docs: обновлена документация`
- ❌ `fix bug`
- ❌ `update`

### 7.3. CHANGELOG

- Всегда обновляйте CHANGELOG.md перед релизом
- Группируйте изменения по типам: Добавлено, Изменено, Исправлено, Удалено
- Используйте понятные описания для пользователей

### 7.4. Тестирование

- Тестируйте код перед созданием релиза
- Проверяйте работу на staging окружении (если есть)
- Убедитесь, что все зависимости обновлены

---

## 8. Примеры

### 8.1. Создание первого релиза (4.0.0)

```bash
# 1. Обновить CHANGELOG.md - переместить все из [Unreleased] в [4.0.0]
# 2. Обновить README.md с версией 4.0.0
# 3. Закоммитить изменения
git add CHANGELOG.md README.md
git commit -m "chore: подготовка к первому релизу v4.0.0"
git push origin main

# 4. Создать тег
git tag -a v4.0.0 -m "Release version 4.0.0

Первый релиз проекта Liza Party Bot.

Основные возможности:
- Управление плейлистами Яндекс.Музыки
- Множественные плейлисты
- Шаринг плейлистов
- Режим технических работ
- Поддержка PostgreSQL и SQLite"

# 5. Отправить тег
git push origin v4.0.0

# 6. Создать релиз на GitHub (через веб-интерфейс)
```

### 8.2. Создание патч-релиза (4.1.0 → 4.1.1)

```bash
# 1. Обновить CHANGELOG.md
# 2. Обновить README.md
# 3. Закоммитить изменения
git add CHANGELOG.md README.md
git commit -m "chore: подготовка к релизу v4.1.1"
git push origin main

# 4. Создать тег
git tag -a v4.1.1 -m "Release version 4.1.1

- Исправлена ошибка парсинга ссылок
- Улучшена обработка ошибок API"

# 5. Отправить тег
git push origin v4.1.1

# 6. Создать релиз на GitHub (через веб-интерфейс)
```

### 8.3. Создание минорного релиза (4.0.0 → 4.1.0)

```bash
# 1. Обновить CHANGELOG.md
# 2. Обновить README.md
# 3. Закоммитить изменения
git add CHANGELOG.md README.md
git commit -m "chore: подготовка к релизу v4.1.0"
git push origin main

# 4. Создать тег
git tag -a v4.1.0 -m "Release version 4.1.0

- Добавлена функция статистики плейлистов
- Улучшен UX команд бота
- Исправлены ошибки в работе с базой данных"

# 5. Отправить тег
git push origin v4.1.0

# 6. Создать релиз на GitHub (через веб-интерфейс)
```

---

## 9. Автоматизация (опционально)

### 9.1. Скрипт для создания релиза

Создайте скрипт `scripts/create_release.sh`:

```bash
#!/bin/bash

# Использование: ./scripts/create_release.sh 4.1.0

VERSION=$1

if [ -z "$VERSION" ]; then
    echo "Использование: $0 <version>"
    echo "Пример: $0 4.1.0"
    exit 1
fi

# Проверка формата версии
if ! [[ $VERSION =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
    echo "Ошибка: неверный формат версии. Используйте формат X.Y.Z"
    exit 1
fi

echo "Создание релиза v$VERSION..."

# Обновление README.md (если нужно)
# sed -i '' "s/> \*\*Версия\*\*: .*/> **Версия**: $VERSION/" README.md

# Создание тега
git tag -a "v$VERSION" -m "Release version $VERSION"

# Отправка тега
git push origin "v$VERSION"

echo "✅ Релиз v$VERSION создан!"
echo "📝 Не забудьте создать релиз на GitHub: https://github.com/your-username/Liza_patry_bot/releases/new"
```

**Использование:**

```bash
chmod +x scripts/create_release.sh
./scripts/create_release.sh 4.1.0
```

---

## 10. Чеклист перед релизом

- [ ] Все изменения закоммичены и запушены
- [ ] Код протестирован
- [ ] CHANGELOG.md обновлен (записи перемещены из [Unreleased] в версию)
- [ ] README.md обновлен (версия)
- [ ] Создан тег: `git tag -a vX.Y.Z -m "Release X.Y.Z"`
- [ ] Тег отправлен: `git push origin vX.Y.Z`
- [ ] Создан релиз на GitHub с описанием из CHANGELOG.md
- [ ] Проверено автоматическое развертывание (если настроено)

---

## 11. Заключение

Правильный процесс релизов позволяет:
- ✅ Отслеживать версии кода
- ✅ Контролировать развертывание
- ✅ Упрощать откат к предыдущим версиям
- ✅ Документировать изменения
- ✅ Улучшать коммуникацию с пользователями

**Следующие шаги:**
1. Обновите CHANGELOG.md с текущими изменениями
2. Создайте первый релиз v4.0.0 (если еще не создан)
3. Настройте автоматическое развертывание релизов (опционально)
4. Следуйте процессу при каждом релизе

---

**Документ создан:** 2025-12-06  
**Версия:** 1.0  
**Автор:** AI Agent

