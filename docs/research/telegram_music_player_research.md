# Исследование: Воспроизведение музыки в Telegram через Web Apps

**Дата**: 2025-12-06
**Статус**: 🔍 Исследование  
**Связанная задача**: `docs/tasks/pending/task-feature-auto-queue-update.md`

## Резюме

Исследование возможности воспроизведения музыки прямо из Telegram через встроенный плеер с использованием Telegram Web Apps (Mini Apps). Это решение позволит контролировать воспроизведение и автоматически обновлять очередь при изменении плейлиста.

## Telegram Web Apps / Mini Apps

### Что это такое?

Telegram Web Apps (также называемые Mini Apps) — это веб-приложения, которые запускаются внутри Telegram-клиента в отдельном окне. Они предоставляют расширенный интерфейс и функциональность без необходимости покидать приложение.

**Ключевые особенности:**
- Открываются в отдельном окне внутри Telegram
- Полный доступ к Telegram Web App API для взаимодействия с ботом
- Поддержка JavaScript и современных веб-технологий
- Адаптивный дизайн для мобильных и десктопных клиентов

### Как добавить Web App в бота

#### Способ 1: Через BotFather (кнопка "Open")

1. Перейти в чат с [@BotFather](https://t.me/BotFather)
2. Отправить команду `/setmenubutton` или `/mybots` → выбрать бота → "Bot Settings" → "Configure Mini App"
3. Указать URL веб-приложения
4. Указать название кнопки (например, "🎵 Открыть плеер")
5. Кнопка появится в профиле бота и в списке чатов

#### Способ 2: Через inline-кнопки в коде

```python
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton, WebAppInfo

# Создание кнопки с Web App
web_app_button = InlineKeyboardButton(
    text="🎵 Открыть плеер",
    web_app=WebAppInfo(url="https://your-web-app-url.com")
)

keyboard = InlineKeyboardMarkup(inline_keyboard=[[web_app_button]])
```

#### Способ 3: Через Reply-клавиатуру

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton, WebAppInfo

web_app_button = KeyboardButton(
    text="🎵 Открыть плеер",
    web_app=WebAppInfo(url="https://your-web-app-url.com")
)

keyboard = ReplyKeyboardMarkup(
    keyboard=[[web_app_button]],
    resize_keyboard=True
)
```

### Telegram Web App API

Web App может взаимодействовать с ботом через JavaScript API:

```javascript
// Инициализация Web App
window.Telegram.WebApp.ready();

// Отправка данных боту
window.Telegram.WebApp.sendData(JSON.stringify({
    action: 'play_track',
    track_id: '123456'
}));

// Получение данных от бота
window.Telegram.WebApp.onEvent('mainButtonClicked', () => {
    // Обработка клика
});

// Закрытие Web App
window.Telegram.WebApp.close();
```

**Основные методы API:**
- `window.Telegram.WebApp.ready()` - инициализация
- `window.Telegram.WebApp.sendData(data)` - отправка данных боту
- `window.Telegram.WebApp.onEvent(event, callback)` - подписка на события
- `window.Telegram.WebApp.close()` - закрытие Web App
- `window.Telegram.WebApp.expand()` - развернуть на весь экран
- `window.Telegram.WebApp.MainButton` - управление главной кнопкой
- `window.Telegram.WebApp.BackButton` - управление кнопкой "Назад"

## Воспроизведение музыки в Telegram

### Telegram Bot API sendAudio

Telegram поддерживает отправку аудиофайлов через метод `sendAudio`:

```python
from aiogram.types import FSInputFile

# Отправка аудиофайла
await bot.send_audio(
    chat_id=chat_id,
    audio=FSInputFile("track.mp3"),
    title="Название трека",
    performer="Исполнитель",
    duration=180  # в секундах
)
```

**Особенности:**
- ✅ Встроенный плеер Telegram с поддержкой заблокированного экрана
- ✅ Управление через уведомления (play/pause/next)
- ✅ Отображение обложки трека
- ❌ Нужно скачивать файлы (проблема с правами на контент)
- ❌ Ограничения на размер файлов (до 50 МБ для ботов)

### Воспроизведение в Web App

В Web App можно использовать HTML5 Audio API:

```javascript
const audio = new Audio('https://example.com/track.mp3');
audio.play();
audio.pause();

// События
audio.addEventListener('loadedmetadata', () => {
    console.log('Длительность:', audio.duration);
});

audio.addEventListener('timeupdate', () => {
    console.log('Текущее время:', audio.currentTime);
});
```

**Особенности:**
- ✅ Полный контроль над воспроизведением
- ✅ Можно стримить аудио (не обязательно скачивать)
- ✅ Гибкий UI
- ⚠️ Воспроизведение на заблокированном экране зависит от браузера/платформы
- ⚠️ Нужно получать прямые ссылки на аудиофайлы

## Интеграция с Яндекс.Музыкой

### Получение ссылок на аудиофайлы

**Вопросы для исследования:**
1. Можно ли получить прямые ссылки на аудиофайлы через API Яндекс.Музыки?
2. Есть ли ограничения на использование аудиофайлов (права, DRM)?
3. Как получить URL для стриминга треков?
4. Поддерживает ли API получение preview треков?

**Текущее состояние:**
- Бот использует библиотеку `yandex_music` для работы с API
- Методы получения треков есть, но нужно проверить возможность получения прямых ссылок на аудио

### Возможные подходы

1. **Прямые ссылки через API** (если доступно)
   - Получить URL трека через API
   - Использовать для стриминга в Web App или отправки через sendAudio

2. **Прокси-сервер** (если прямые ссылки недоступны)
   - Скачивать треки на сервер бота
   - Предоставлять через прокси для Web App
   - ⚠️ Проблемы с правами на контент и хранением

3. **Интеграция с Яндекс.Музыкой через OAuth**
   - Пользователь авторизуется в Яндекс.Музыке
   - Web App использует токен для получения треков
   - ⚠️ Сложность реализации и безопасность

## Примеры на GitHub

### Рекомендации для поиска

Искать репозитории с ключевыми словами:
- "telegram bot music player"
- "telegram mini app music"
- "telegram web app audio player"
- "telegram bot audio streaming"

### Что искать в примерах

1. **Структура Web App**
   - Как организован код (HTML/CSS/JS)
   - Использование Telegram Web App API
   - Интеграция с ботом

2. **Аудиоплеер**
   - Реализация HTML5 Audio
   - Управление очередью воспроизведения
   - Обработка ошибок и переподключений

3. **Коммуникация с ботом**
   - Отправка данных из Web App в бот
   - Получение обновлений от бота
   - Webhook/пуш-уведомления

## Технические требования

### Форматы аудио

Telegram поддерживает:
- MP3 (рекомендуется)
- OGG
- M4A
- FLAC (ограниченная поддержка)

### Ограничения

- **Размер файла**: до 50 МБ для ботов (20 МБ для обычных пользователей)
- **Длительность**: без ограничений
- **Битрейт**: рекомендуется 128-320 kbps для баланса качества и размера

### Архитектура решения

```
┌─────────────┐
│  Telegram   │
│     Bot     │
└──────┬──────┘
       │
       ├─── Web App (HTML/CSS/JS)
       │    ├── Аудиоплеер
       │    ├── Список треков
       │    └── Управление воспроизведением
       │
       └─── Backend (Python/aiogram)
            ├── API для получения треков
            ├── Управление плейлистами
            └── Webhook для обновлений
```

## План дальнейших действий

1. **Исследование API Яндекс.Музыки**
   - Проверить возможность получения прямых ссылок на треки
   - Изучить документацию библиотеки `yandex_music`
   - Протестировать получение аудиофайлов

2. **Поиск примеров**
   - Найти репозитории на GitHub с музыкальными плеерами
   - Изучить их реализацию
   - Адаптировать под наш бот

3. **Прототип Web App**
   - Создать базовую структуру Web App
   - Реализовать простой аудиоплеер
   - Интегрировать с ботом

4. **Тестирование**
   - Проверить работу на разных платформах
   - Тестировать воспроизведение на заблокированном экране
   - Проверить обновление очереди в реальном времени

## Ресурсы

- [Telegram Bot API - Web Apps](https://core.telegram.org/bots/webapps)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/api#inline-mode)
- [Telegram Web App API Reference](https://core.telegram.org/bots/webapps#initializing-mini-apps)
- [Habr: Telegram Web Apps](https://habr.com/ru/articles/666278/)

## Выводы

Telegram Web Apps предоставляют мощный инструмент для создания музыкального плеера внутри Telegram. Основные преимущества:

1. ✅ Полный контроль над воспроизведением
2. ✅ Возможность автоматического обновления очереди
3. ✅ Удобный UI в отдельном окне
4. ✅ Интеграция с ботом через Web App API

**Основной вопрос для исследования**: Можно ли получить прямые ссылки на аудиофайлы через API Яндекс.Музыки для стриминга в Web App или отправки через sendAudio?

