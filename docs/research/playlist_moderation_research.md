# Исследование модерации названий плейлистов и обработка ошибок API

**Дата**: 2024  
**Статус**: ✅ Завершено  
**Связанная задача**: [task-research-playlist-moderation.md](../tasks/pending/task-research-playlist-moderation.md)

## Цель исследования

Исследовать механизм модерации названий плейлистов в API Яндекс.Музыки и улучшить обработку ошибок при создании и редактировании плейлистов для защиты аккаунта от блокировки.

## Текущее состояние

### Анализ кода

#### 1. Создание плейлиста (`yandex_client_manager.py`)

**Метод**: `create_playlist()`

```python
async def create_playlist(self, telegram_id: Optional[int], title: str) -> Optional[Dict]:
    try:
        # ...
        playlist = client.users_playlists_create(title)
        # ...
    except Exception as e:
        logger.exception(f"Ошибка при создании плейлиста: {e}")
        return None
```

**Проблемы**:
- Все исключения обрабатываются общим `except Exception`
- Нет различения типов ошибок (модерация, сеть, авторизация и т.д.)
- Пользователю показывается общее сообщение без деталей
- Нет возможности повторить попытку с другим названием

#### 2. Редактирование названия плейлиста (`services/yandex_service.py`)

**Метод**: `set_playlist_name()`

```python
def set_playlist_name(self, playlist_kind: str, owner_id: str, new_name: str, max_retries: int = 2) -> Tuple[bool, Optional[str]]:
    for attempt in range(max_retries):
        try:
            # ...
            result = self.client._request.post(url)
            # ...
        except Exception as e:
            error_msg = str(e).lower()
            # Проверка только на revision ошибки
            if ("wrong-revision" in error_msg or "revision" in error_msg) and attempt < max_retries - 1:
                continue
            return False, f"Ошибка изменения имени: {e}"
```

**Проблемы**:
- Обрабатываются только ошибки revision
- Ошибки модерации не различаются от других ошибок
- Сообщение об ошибке передается пользователю как есть (может содержать технические детали)

#### 3. Валидация на стороне бота (`handlers/commands.py`)

**Метод**: `create_playlist_name()`

```python
# Валидация
if not title:
    # ...
if len(title) > 100:
    # ...
```

**Текущие проверки**:
- ✅ Проверка на пустое название
- ✅ Проверка длины (максимум 100 символов)
- ❌ Нет проверки на запрещенные слова/символы
- ❌ Нет проверки на специальные символы

## Исследование API Яндекс.Музыки

### Типы исключений библиотеки yandex-music

Библиотека `yandex-music` использует следующие типы исключений:

1. **`YandexMusicError`** - базовое исключение для всех ошибок API
2. **`TimedOutError`** - ошибки таймаута
3. **`NetworkError`** - сетевые ошибки
4. **`UnauthorizedError`** - ошибки авторизации (401)
5. **`BadRequestError`** - ошибки запроса (400)
6. **`ForbiddenError`** - ошибки доступа (403)
7. **`NotFoundError`** - ресурс не найден (404)

### Возможные ошибки при создании/редактировании плейлиста

#### 1. Ошибки модерации названия

**Гипотеза**: API может возвращать ошибки при попытке создать плейлист с некорректным названием.

**Возможные сценарии**:
- Название содержит запрещенные слова (матерные, оскорбительные)
- Название содержит спам/рекламу
- Название нарушает правила сообщества
- Название слишком длинное (хотя проверяется на стороне бота)
- Название содержит специальные символы в неправильном формате

**Ожидаемое поведение API**:
- Вероятно, возвращает HTTP 400 (BadRequest) или 403 (Forbidden)
- Может содержать сообщение об ошибке в теле ответа
- Может содержать код ошибки (например, "moderation-failed", "invalid-name")

#### 2. Ошибки доступа

- Недостаточно прав для создания плейлиста
- Превышен лимит плейлистов (хотя проверяется на стороне бота)

#### 3. Сетевые ошибки

- Таймаут запроса
- Проблемы с подключением

### Формат ошибок API

**Предполагаемый формат ответа при ошибке**:

```json
{
  "error": {
    "name": "BadRequest",
    "message": "Название плейлиста не прошло модерацию",
    "code": "MODERATION_FAILED"
  }
}
```

Или в виде HTTP статус кода:
- `400 Bad Request` - некорректное название
- `403 Forbidden` - недостаточно прав или модерация не пройдена
- `429 Too Many Requests` - слишком много запросов

## Рекомендации по улучшению

### 1. Улучшение обработки ошибок при создании плейлиста

#### 1.1. Различение типов ошибок

```python
async def create_playlist(self, telegram_id: Optional[int], title: str) -> Tuple[Optional[Dict], Optional[str]]:
    """
    Returns:
        Tuple[Optional[Dict], Optional[str]]: (результат, сообщение об ошибке)
        Если результат None, то error содержит понятное сообщение для пользователя
    """
    client = await self.get_client(telegram_id)
    
    try:
        # ...
        playlist = client.users_playlists_create(title)
        # ...
        return result, None
    except BadRequestError as e:
        # Ошибка 400 - вероятно, проблема с названием
        error_msg = str(e).lower()
        if any(keyword in error_msg for keyword in ['moderation', 'name', 'title', 'invalid']):
            return None, "Название плейлиста не прошло модерацию. Пожалуйста, используйте другое название."
        return None, "Некорректный запрос. Проверьте название плейлиста."
    except ForbiddenError as e:
        # Ошибка 403 - недостаточно прав или модерация
        return None, "Недостаточно прав для создания плейлиста или название не прошло модерацию."
    except UnauthorizedError as e:
        # Ошибка 401 - проблема с токеном
        return None, "Токен Яндекс.Музыки недействителен. Используйте /set_token для обновления."
    except TimedOutError as e:
        # Таймаут
        return None, "Превышено время ожидания ответа. Попробуйте еще раз."
    except NetworkError as e:
        # Сетевые ошибки
        return None, "Проблема с подключением к Яндекс.Музыке. Попробуйте позже."
    except YandexMusicError as e:
        # Другие ошибки API
        logger.warning(f"Неизвестная ошибка API при создании плейлиста: {e}")
        return None, "Ошибка при создании плейлиста. Попробуйте еще раз."
    except Exception as e:
        # Неожиданные ошибки
        logger.exception(f"Неожиданная ошибка при создании плейлиста: {e}")
        return None, "Произошла ошибка при создании плейлиста."
```

#### 1.2. Улучшение обработчика в боте

```python
async def create_playlist_name(self, message: Message, state: FSMContext):
    # ... валидация ...
    
    result, error = await self.client_manager.create_playlist(telegram_id, title)
    
    if result:
        # Успех
        # ...
    else:
        # Ошибка - показываем понятное сообщение
        error_message = error or "Не удалось создать плейлист."
        
        # Если ошибка связана с модерацией, предлагаем ввести другое название
        if "модерац" in error_message.lower() or "название" in error_message.lower():
            await message.answer(
                f"❌ {error_message}\n\n"
                f"💡 Попробуйте ввести другое название:",
                reply_markup=get_cancel_keyboard()
            )
            # Не очищаем state - пользователь может попробовать еще раз
            return
        
        # Для других ошибок показываем сообщение и очищаем state
        await message.answer(
            f"❌ {error_message}",
            reply_markup=get_main_menu_keyboard()
        )
        await state.clear()
```

### 2. Улучшение обработки ошибок при редактировании названия

#### 2.1. Различение типов ошибок в `set_playlist_name()`

```python
def set_playlist_name(
    self,
    playlist_kind: str,
    owner_id: str,
    new_name: str,
    max_retries: int = 2
) -> Tuple[bool, Optional[str]]:
    """
    Returns:
        Tuple[bool, Optional[str]]: (успех, понятное сообщение об ошибке)
    """
    for attempt in range(max_retries):
        try:
            # ...
            result = self.client._request.post(url)
            # ...
            return True, None
        except BadRequestError as e:
            error_msg = str(e).lower()
            if any(keyword in error_msg for keyword in ['moderation', 'name', 'title', 'invalid']):
                return False, "Название плейлиста не прошло модерацию. Пожалуйста, используйте другое название."
            return False, "Некорректное название плейлиста."
        except ForbiddenError as e:
            return False, "Недостаточно прав для изменения названия или название не прошло модерацию."
        except Exception as e:
            error_msg = str(e).lower()
            # Проверка на revision ошибки
            if ("wrong-revision" in error_msg or "revision" in error_msg) and attempt < max_retries - 1:
                continue
            return False, f"Ошибка изменения имени: {e}"
    
    return False, "Не удалось изменить название после нескольких попыток."
```

#### 2.2. Улучшение обработчика редактирования

В `handlers/callbacks.py` или соответствующем обработчике:

```python
async def _handle_edit_playlist_name(self, query: CallbackQuery, playlist_id: int, telegram_id: int):
    # ... получение нового названия ...
    
    ok, error = await self.playlist_service.edit_playlist_name(playlist_id, new_name, telegram_id)
    
    if ok:
        # Успех
        # ...
    else:
        error_message = error or "Не удалось изменить название."
        
        # Если ошибка связана с модерацией, предлагаем ввести другое название
        if "модерац" in error_message.lower() or "название" in error_message.lower():
            await edit_message(
                query,
                f"❌ {error_message}\n\n"
                f"💡 Попробуйте ввести другое название:"
            )
            # Возвращаем в состояние редактирования
            await state.set_state(EditNameStates.waiting_edit_name)
            await state.update_data(edit_playlist_id=playlist_id)
            return
        
        # Для других ошибок
        await edit_message(query, f"❌ {error_message}")
```

### 3. Предварительная валидация названий

#### 3.1. Базовые проверки (уже реализованы)

- ✅ Проверка на пустое название
- ✅ Проверка длины (максимум 100 символов)

#### 3.2. Дополнительные проверки (рекомендуется добавить)

```python
def validate_playlist_name(name: str) -> Tuple[bool, Optional[str]]:
    """
    Валидация названия плейлиста.
    
    Returns:
        Tuple[bool, Optional[str]]: (валидно, сообщение об ошибке)
    """
    if not name or not name.strip():
        return False, "Название не может быть пустым."
    
    if len(name) > 100:
        return False, "Название слишком длинное (максимум 100 символов)."
    
    # Проверка на только пробелы
    if not name.strip():
        return False, "Название не может состоять только из пробелов."
    
    # Проверка на слишком много пробелов подряд
    if "  " in name:
        return False, "Название не должно содержать несколько пробелов подряд."
    
    # Проверка на управляющие символы (кроме разрешенных)
    # Разрешаем: буквы, цифры, пробелы, дефисы, подчеркивания, скобки, кавычки
    import re
    allowed_pattern = re.compile(r'^[\w\s\-\(\)\[\]\"\'«».,!?;:]+$', re.UNICODE)
    if not allowed_pattern.match(name):
        return False, "Название содержит недопустимые символы."
    
    return True, None
```

**Примечание**: Полный список запрещенных слов невозможно получить заранее, так как он:
- Может изменяться со временем
- Может зависеть от контекста
- Может быть специфичен для разных регионов

Поэтому предварительная валидация может только уменьшить вероятность ошибок, но не гарантировать успех.

### 4. Логирование ошибок модерации

Для мониторинга и анализа рекомендуется логировать ошибки модерации отдельно:

```python
if "модерац" in error_message.lower():
    logger.warning(
        f"Ошибка модерации названия плейлиста: title='{title}', "
        f"user_id={telegram_id}, error={str(e)}"
    )
```

Это поможет:
- Отслеживать частоту ошибок модерации
- Выявлять проблемные названия
- Анализировать паттерны ошибок

## План реализации

### Этап 1: Улучшение обработки ошибок в `yandex_client_manager.py`

- [ ] Импортировать специфичные исключения из `yandex_music.exceptions`
- [ ] Изменить сигнатуру `create_playlist()` для возврата кортежа `(result, error)`
- [ ] Добавить обработку различных типов исключений
- [ ] Добавить понятные сообщения об ошибках для пользователей

### Этап 2: Улучшение обработки ошибок в `services/yandex_service.py`

- [ ] Изменить `set_playlist_name()` для различения типов ошибок
- [ ] Добавить обработку ошибок модерации
- [ ] Улучшить сообщения об ошибках

### Этап 3: Улучшение обработчиков в боте

- [ ] Обновить `create_playlist_name()` для обработки новых типов ошибок
- [ ] Добавить возможность повторного ввода названия при ошибке модерации
- [ ] Обновить обработчик редактирования названия

### Этап 4: Добавление предварительной валидации (опционально)

- [ ] Создать функцию `validate_playlist_name()` в `utils/`
- [ ] Добавить проверки на недопустимые символы
- [ ] Интегрировать валидацию в обработчики

### Этап 5: Тестирование

- [ ] Протестировать создание плейлиста с различными названиями
- [ ] Протестировать обработку ошибок модерации (если возможно)
- [ ] Протестировать обработку других типов ошибок
- [ ] Проверить логирование ошибок

## Выводы

1. **Текущее состояние**: Обработка ошибок при создании и редактировании плейлистов недостаточно детализирована. Все ошибки обрабатываются общим образом, что не позволяет различать ошибки модерации от других типов ошибок.

2. **Риски**: 
   - Пользователи могут не понимать, почему не удалось создать/изменить плейлист
   - Нет возможности повторить попытку с другим названием при ошибке модерации
   - Аккаунт может быть заблокирован при множественных попытках создать плейлист с некорректным названием

3. **Рекомендации**:
   - Различать типы ошибок API и обрабатывать их соответствующим образом
   - Предоставлять понятные сообщения пользователям
   - Давать возможность повторить попытку при ошибке модерации
   - Добавить предварительную валидацию названий (базовые проверки)
   - Логировать ошибки модерации для анализа

4. **Ограничения исследования**:
   - Невозможно точно определить формат ошибок API без реальных тестов
   - Список запрещенных слов/символов неизвестен и может изменяться
   - Модерация может работать по-разному в зависимости от региона и аккаунта

## Следующие шаги

1. Реализовать улучшения обработки ошибок согласно плану
2. Протестировать на реальных данных (с осторожностью, чтобы не нарушить правила)
3. Мониторить логи ошибок модерации
4. При необходимости уточнить формат ошибок API на основе реальных данных

